Place avatar images here for the JavaFX chatbot UI:
- DaUser.png
- DaDuke.png

Main.java loads these from:
/images/DaUser.png
/images/DaDuke.png

If they are missing, the app uses a placeholder image so it still runs.
